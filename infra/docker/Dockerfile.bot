FROM rust:1.76 AS builder
WORKDIR /app
COPY . .
RUN cargo build --release -p kyodoku-bot || true

FROM debian:bookworm-slim
WORKDIR /app
COPY --from=builder /app/target/release/kyodoku-bot /usr/local/bin/kyodoku-bot
CMD ["kyodoku-bot"]
